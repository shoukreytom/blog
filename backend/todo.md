# TODOs of backend (Later)
1) use celery to update & delete unused data.
2) in notifications add these urls:
    * `<str:username>/notifications/markall/read/`
    * `<str:username>/notifications/markall/unread/`